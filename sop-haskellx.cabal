name: sop-haskellx
version: 2017
build-type: Simple
cabal-version: >= 1.10

library
  hs-source-dirs: src
  default-language: Haskell2010
  build-depends:
    base >= 4.9 && < 5,
    generics-sop >= 0.3.1 && < 0.4
  exposed-modules:
    Exercises
